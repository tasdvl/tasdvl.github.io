import React from 'react';

export default function ColorScheme() {
    return(<div className="w-full h-12 mt-10 bg-[repeating-linear-gradient(90deg, #000, #000 10px, #fff 10px, #fff 20px)]">
        {/* This creates a checkerboard pattern of black and white */}
      </div>
      );
}
